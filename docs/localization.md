# Локализация интерфейса

Каталоги находятся в `src/SellerFinance.Web/src/i18n.tsx`. Русский каталог определяет тип `TranslationKey`, а остальные языки обязаны реализовать `Record<TranslationKey, string>`. Поэтому пропущенный ключ останавливает TypeScript/CI build.

Компоненты получают строки через `useI18n().t(key)`. Выбранный язык сохраняется в `localStorage` под ключом `seller-finance-locale`, а `<html lang>` обновляется автоматически.

Переведены auth, восстановление доступа, header, основная навигация, dashboard, основной экран заказов с финансовым drill-down и каталог товаров с фильтрами/сортировкой. Русский и казахский каталоги для этих экранов типизированы одним набором ключей. Карточка товара, импорт и остальные продуктовые экраны переносятся последовательно; новые строки нельзя добавлять напрямую в уже локализованные компоненты.
